
#
# Regex Expression recommendations:
# - Use ^ at the start
# - Use $ at the end
# - Be carefull of potential arguments in URI's
#

[filters.get]
allowed = false
regex = ''

[filters.head]
allowed = false
regex = ''

[filters.post]
allowed = false
regex = ''

[filters.put]
allowed = false
regex = ''

[filters.patch]
allowed = false
regex = ''

[filters.delete]
allowed = false
regex = ''